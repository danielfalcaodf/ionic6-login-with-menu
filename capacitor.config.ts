import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic6-login-with-menu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
